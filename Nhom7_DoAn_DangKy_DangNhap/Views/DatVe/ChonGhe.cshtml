@model IEnumerable<Nhom7_DoAn_DangKy_DangNhap.Models.Ghe>
@{
    var suat = ViewBag.SuatChieu;
    var gheDaDat = (List<string>)ViewBag.GheDaDat;
}
<h2>Chọn ghế cho phim: @suat.Phim.TenPhim</h2>
<p>Ngày: @suat.NgayChieu.ToString("dd/MM/yyyy") - Giờ: @suat.ThoiGianChieu</p>

<form asp-action="ThanhToanForm" asp-controller="DatVe" method="post">
    <input type="hidden" name="maSuatChieu" value="@suat.MaSuatChieu" />

    <div style="display:flex;flex-wrap:wrap;gap:10px;">
        @foreach (var ghe in Model)
        {
            string cssClass = ghe.MaLoaiGhe switch
            {
                "LG02" => "btn btn-danger",   // VIP
                "LG03" => "btn btn-warning", // Couple
                _ => "btn btn-primary"       // Thường
            };

            bool isDisabled = gheDaDat.Contains(ghe.TenGhe);
            <label class="@cssClass" style="width:60px;text-align:center;">
                <input type="checkbox" name="gheDaChon" value="@ghe.TenGhe" @(isDisabled ? "disabled" : "") />
                @ghe.TenGhe
            </label>
        }
    </div>

    <div class="mt-3">
        <label>Chọn phương thức thanh toán:</label>
        <select name="phuongThuc" class="form-control" required>
            <option value="">-- Chọn --</option>
            <option value="MoMo">Thanh toán qua MoMo</option>
            <option value="VNPay">Thanh toán qua VNPay</option>
            <option value="OTP">Thanh toán qua OTP</option>
        </select>
    </div>

    <button type="submit" class="btn btn-success mt-3">Thanh toán</button>
</form>
